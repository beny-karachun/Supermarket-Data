# il-supermarket-scraper's own deps, listed explicitly because the library
# pins lxml<6 which has no Python 3.14 wheels; lxml 6 is API-compatible here.
# Install order matters: deps first, then the scraper with --no-deps
# (see scripts/setup_venv.sh / README).
retry>=0.9.2,<1
mock>=4.0.3,<5
requests>=2.32.4,<3
lxml>=5.2.1
beautifulsoup4>=4.10.0,<5
pymongo>=4.6.3,<5
dnspython>=2.6.1,<3
pytz>=2022.4
holidays>=0.45,<1
cachetools>=5.2.0,<6
pytest-playwright>=0.7.0,<1
aioftp>=0.22.3
pydantic>=2.10.4

pytest>=8.0
